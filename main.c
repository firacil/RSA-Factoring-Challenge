#include "main.h"

/**
 * main - find factors of number.
 * @argc: arument count
 * @argv: arguments
 * Return: integer.
 */

int main(int argc, char **argv)
{
	(void) argv;

	if (argc != 2)
	{
		fprintf(stderr, "File Not Found\n");
		exit(EXIT_FAILURE);
	}

	return (0);

}
